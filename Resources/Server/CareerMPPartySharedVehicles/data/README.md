This folder is reserved for persistent JSON state used by the future
CareerMP Party Shared Vehicles module.

Expected files later:

- `state.json`

This README exists so the directory is preserved in the workspace.
